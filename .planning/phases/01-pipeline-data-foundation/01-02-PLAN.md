---
phase: 01-pipeline-data-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/routers/fragrances.py
  - frontend/src/lib/api.ts
  - frontend/src/lib/hooks.ts
autonomous: true
requirements:
  - PIPE-02
user_setup: []

must_haves:
  truths:
    - "recommend_by_text POST endpoint returns 404 (not 503) — removed from router"
    - "recommend_by_profile POST endpoint returns 404 (not 503) — removed from router"
    - "get_recommendation_result GET endpoint returns 404 — removed (D-14)"
    - "get_recommendation_weekly_metrics GET endpoint returns 404 — removed (D-15)"
    - "Frontend text search calls /fragrances/catalog?q=... instead of recommend/text polling"
  artifacts:
    - path: backend/app/routers/fragrances.py
      provides: "4 dead endpoints removed, router file shortened"
      min_lines: 700
    - path: frontend/src/lib/api.ts
      provides: "getFragranceCatalog uses direct catalog call for text search"
      min_lines: 140
  key_links:
    - from: frontend/src/lib/api.ts
      to: backend/app/routers/fragrances.py
      via: "getFragranceCatalog calls /fragrances/catalog?q= for text search"
      pattern: "/fragrances/catalog"
---

<objective>
Remove 4 dead 503 endpoints from fragrances.py and scrub corresponding frontend code.

**Purpose:** PIPE-02 requires removing or implementing the dead 503 endpoints. The research pivot mean these endpoints have no ongoing utility — clean removal is correct. The frontend's text search currently polls the dead recommend/text endpoint; rewriting it to directly call `/fragrances/catalog?q=...` makes it functional immediately.

**Output:** Clean fragrances.py without the 4 dead endpoints, frontend api.ts with direct catalog text search, cleaned schemas if needed.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-pipeline-data-foundation/01-CONTEXT.md
@.planning/phases/01-pipeline-data-foundation/01-RESEARCH.md

# Interface context: key types and contracts for this plan
# All referenced from actual backend/app/routers/fragrances.py and frontend/src/lib/api.ts

Backend schemas used (from schemas.py):
- TextRecommendationRequest — POST body schema (can be removed with endpoint, but removing unused imports is not strictly necessary — keep imports to minimize diff)
- RecommendationJob — returned by removed endpoints (can remove schema import if no other usage)
- RecommendationResult — returned by get_recommendation_result (can remove if no other usage)
- RecommendationWeeklyMetrics — returned by weekly metrics (can remove if no other usage)

Frontend api.ts patterns:
- getFragranceCatalog(limit, offset, filters?) — currently has if(filters?.q) branch calling /fragrances/recommend/text
- The /fragrances/catalog endpoint (get_catalog router) accepts q= parameter for text search (lines 233-301 of fragrances.py)
</context>

<tasks>

<task type="auto">
  <name>task 1: remove 4 dead endpoints from fragrances.py</name>
  <files>backend/app/routers/fragrances.py</files>
  <read_first>
    backend/app/routers/fragrances.py:
    - recommend_by_text() endpoint (lines 504-546) — entire function + @router decorator
    - get_recommendation_weekly_metrics() endpoint (lines 695-784) — entire function + @router decorator (D-15)
    - get_recommendation_result() endpoint (lines 787-864) — entire function + @router decorator (D-14)
    - recommend_by_profile() endpoint (lines 867-916) — entire function + @router decorator
    - Imports at top of file (lines 10-43) — check which are only used by removed endpoints
  </read_first>
  <action>
    Remove exactly these 4 endpoint functions with their @router decorators and docstrings:

    1. **recommend_by_text()** (lines 504-546): Delete from `@router.post("/recommend/text", ...)` line through the closing of the function body. The function ends with `return {"status": "success", "data": data}` at line 546, followed by a blank line before `@router.post("/recommend/interactions", ...)` at line 549.

    2. **get_recommendation_weekly_metrics()** (lines 695-784): Delete from `@router.get("/recommend/metrics/weekly", ...)` line through the closing `)` at line 784. The function body ends with `)` on line 784, followed by a blank line before `@router.get("/recommend/{job_id}", ...)` at line 787.

    3. **get_recommendation_result()** (lines 787-864): Delete from `@router.get("/recommend/{job_id}", ...)` line through line 864. The function ends with `raise HTTPException(...)` at line 864, followed by a blank line before `@router.post("/recommend/profile", ...)` at line 867.

    4. **recommend_by_profile()** (lines 867-916): Delete from `@router.post("/recommend/profile", ...)` line through line 916. The function ends with `return {"status": "success", "data": data}` at line 916.

    After removing all 4 endpoints, check which imports at the top of the file are no longer used:
    - `from uuid import uuid4` (line 16) — check if uuid4 is used by any remaining code
    - `from app.schemas.schemas import (RecommendationJob, RecommendationResult, RecommendationWeeklyMetrics, TextRecommendationRequest)` — these schemas are only used by removed endpoints; remove them from the import block
    - Keep `RecommendationInteractionBatchRequest, RecommendationInteractionBatchResponse` — used by ingest_recommendation_interactions (line 549)
    - `from app.services.job_store import create_job, get_job, is_job_timed_out, update_job` — check which are still used
    - `from datetime import UTC, datetime, timedelta` — check if timedelta is still used (weekly metrics used it)
    - `from uuid import uuid4` — only used by removed endpoints

    Do NOT modify get_graph_client() (lines 218-230) — that's handled in Plan 01-03.

    Run `ruff format backend/app/routers/fragrances.py` after edits.
  </action>
  <verify>
    <automated>if (Select-String -Path "backend/app/routers/fragrances.py" -Pattern "recommend_by_text" -Quiet) { "FAIL: recommend_by_text still present" } else { "PASS" };& if (Select-String -Path "backend/app/routers/fragrances.py" -Pattern "recommend_by_profile" -Quiet) { "FAIL: recommend_by_profile still present" } else { "PASS" };& if (Select-String -Path "backend/app/routers/fragrances.py" -Pattern "get_recommendation_result" -Quiet) { "FAIL: get_recommendation_result still present" } else { "PASS" };& if (Select-String -Path "backend/app/routers/fragrances.py" -Pattern "get_recommendation_weekly_metrics" -Quiet) { "FAIL: get_recommendation_weekly_metrics still present" } else { "PASS" }</automated>
  </verify>
  <done>4 endpoint functions removed from fragrances.py. Unused imports cleaned. ruff format passes.</done>
</task>

<task type="auto">
  <name>task 2: scrub frontend recommend/text calls from api.ts</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    frontend/src/lib/api.ts:
    - getFragranceCatalog() (lines 41-72) — full function
    - The `if (filters?.q)` branch (lines 43-61) calls /fragrances/recommend/text with polling loop
    - The catalog fallback (lines 63-66) calls /fragrances/catalog
  </read_first>
  <action>
    Rewrite the `getFragranceCatalog` method to remove the async recommendation polling pattern.

    **Current behavior** (TO REMOVE, lines 43-62):
    ```
    if (filters?.q) {
        // POST to /fragrances/recommend/text
        // poll GET /fragrances/recommend/{job_id} up to 20 times
        // return results or empty on failure
    }
    ```

    **New behavior**: When `filters?.q` is provided, directly call:
    ```
    GET /fragrances/catalog?q={q}&limit={limit}&offset={offset}
    ```
    This endpoint already supports text search via the `q` parameter (verified in fragrances.py `get_catalog` at line 233-301, `q: str | None = Query(None, min_length=1, max_length=100)`).

    The method signature stays the same. The if/else structure simplifies to:
    - Call `/fragrances/catalog` with params including `q` when filters.q exists
    - Call `/fragrances/catalog` with params excluding `q` when no text filter
    - Return `data` from response (same format as existing non-q path)

    The return type is `{ items: FragranceCatalogItem[], total: number }` — consistent with existing catalog response. No need to change this.

    Remove: polling loop, job_id handling, recommend/text endpoint call. The function should have a single straightforward API call path regardless of whether q is present. Use the same params spread pattern: `params: { limit, offset, ...filters }` in the GET call, where `filters.q` becomes the `q` query param that `/fragrances/catalog` already supports.
  </action>
  <verify>
    <automated>if (Select-String -Path "frontend/src/lib/api.ts" -Pattern "recommend/text" -Quiet) { "FAIL: recommend/text reference still present" } else { "PASS: removed" }</automated>
  </verify>
  <done>getFragranceCatalog no longer calls /fragrances/recommend/text or polls /fragrances/recommend/{job_id}. Text search uses direct /fragrances/catalog?q=... call.</done>
</task>

<task type="auto">
  <name>task 3: verify hooks.ts has no remaining references</name>
  <files>frontend/src/lib/hooks.ts</files>
  <read_first>
    frontend/src/lib/hooks.ts — full file (192 lines)
  </read_first>
  <action>
    Read hooks.ts and verify no references to removed endpoints exist. The `useFragranceCatalog` hook (lines 187-191) calls `api.getFragranceCatalog()` which was fixed in Task 2 — no change needed to the hook itself.

    Check for any direct references to:
    - `/fragrances/recommend/text` — if found, replace with catalog path
    - `/fragrances/recommend/profile` — if found, remove the hook
    - `/fragrances/recommend/{job_id}` — if found, remove the hook

    Based on prior grep: no direct references to removed endpoints exist in hooks.ts. The only reference to the removed flow was through `api.getFragranceCatalog()` which is fixed in Task 2.

    If no changes needed, report "hooks.ts requires no changes" in the task log. Do not modify the file unnecessarily.
  </action>
  <verify>
    <automated>Select-String -Path "frontend/src/lib/hooks.ts" -Pattern "recommend/text|recommend/profile|recommend/\{job_id\}|recommend/[a-z]" -Quiet; if ($LASTEXITCODE -eq 0) { "Check found potential refs, verify manually" } else { "PASS: no direct endpoint refs" }</automated>
  </verify>
  <done>hooks.ts verified clean — no references to removed endpoints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | HTTP requests to removed endpoints now return 404 instead of 503 |
| Frontend → Backend | Text search redirected from recommend/text to catalog endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | E (Info Disclosure) | removed endpoints | accept | Endpoints only returned 503 before — no data exposure |
| T-01-04 | D (DoS) | Frontend redirect | accept | Polling loop (20 retries × 1.5s) eliminated reduces unnecessary load |
</threat_model>

<verification>
1. `grep -c "recommend_by_text\|recommend_by_profile\|get_recommendation_result\|get_recommendation_weekly_metrics" backend/app/routers/fragrances.py` returns 0
2. `grep -c "recommend/text" frontend/src/lib/api.ts` returns 0
3. `ruff check backend/app/routers/fragrances.py` passes
</verification>

<success_criteria>
- GET /fragrances/recommend/{job_id} returns 404 (FastAPI auto-404 for unregistered routes)
- GET /fragrances/recommend/metrics/weekly returns 404
- POST /fragrances/recommend/text returns 404
- POST /fragrances/recommend/profile returns 404
- Frontend text search completes without hitting /fragrances/recommend/text
</success_criteria>

<output>
After completion, create `.planning/phases/01-pipeline-data-foundation/01-02-SUMMARY.md`
</output>
