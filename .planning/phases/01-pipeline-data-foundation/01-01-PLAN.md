---
phase: 01-pipeline-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - backend/pyproject.toml
  - backend/app/sentry_config.py
  - backend/app/services/job_store.py
  - backend/tests/test_celery.py
  - backend/tests/test_recommendation_lifecycle.py
autonomous: true
requirements:
  - PIPE-01
user_setup: []

must_haves:
  truths:
    - "Celery worker container is absent from Docker stack — no more worker crash loops"
    - "Celery dependency removed from pyproject.toml — pip install no longer fetches celery"
    - "No code references to CeleryIntegration or celery_task_id remain in backend app code"
    - "scikit-learn ≥1.3.0 is available in [ml] optional deps for KNN similarity"
    - "Backend tests pass after Celery removal changes"
  artifacts:
    - path: docker-compose.yml
      provides: "Worker container removed, CELERY env vars cleaned from backend service"
      min_lines: 140
    - path: backend/pyproject.toml
      provides: "celery dep removed, scikit-learn>=1.3.0 added to [ml]"
    - path: backend/app/sentry_config.py
      provides: "No CeleryIntegration try/except block"
    - path: backend/app/services/job_store.py
      provides: "celery_task_id field absent from create_job and get_job"
  key_links:
    - from: docker-compose.yml
      to: backend Docker service
      pattern: "No CELERY_BROKER_URL or CELERY_RESULT_BACKEND env vars"
    - from: backend/pyproject.toml
      to: dependencies
      pattern: "No 'celery' in dependencies, 'scikit-learn' in [ml]"
---

<objective>
Remove Celery from the entire codebase and add scikit-learn dependency.

**Purpose:** The Celery worker container crashes on startup because `backend/app/celery_app.py` was never created. Since the research pipeline has no need for async recommendation processing, remove Celery entirely instead of fixing it. Redis stays for caching (D-02). scikit-learn is needed for KNN similarity computation in the ingestor (D-16).

**Output:** Clean docker-compose.yml with no worker service, clean pyproject.toml with no celery dep (scikit-learn added), clean sentry_config.py and job_store.py with no celery references, deleted/cleaned test files.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pipeline-data-foundation/01-CONTEXT.md
@.planning/phases/01-pipeline-data-foundation/01-RESEARCH.md

# Interface context: key files to read first
# docker-compose.yml — worker container lines 100-135, CELERY env vars in backend (lines 70-71)
# backend/pyproject.toml — celery dep at line 22
# backend/app/sentry_config.py — CeleryIntegration lines 33-38
# backend/app/services/job_store.py — celery_task_id lines 52, 84
# backend/tests/test_celery.py — imports from non-existent app.tasks
# backend/tests/test_recommendation_lifecycle.py — celery_task_id in fake_job dicts lines 69, 105, 132
</context>

<tasks>

<task type="auto">
  <name>task 1: remove Celery worker from docker-compose.yml and clean env vars</name>
  <files>docker-compose.yml</files>
  <read_first>
    docker-compose.yml — full file, pay attention to:
    - worker service block (lines 100-135): entire service definition to remove
    - CELERY_BROKER_URL env var in backend service (line 70)
    - CELERY_RESULT_BACKEND env var in backend service (line 71)
  </read_first>
  <action>
    1. Remove the entire `worker:` service block (lines 100-135 inclusive) from docker-compose.yml. The block starts at line 100 with the `  worker:` indented service key and ends at line 135 with the `command:` line. Remove every line from `  worker:` through `    command: celery -A app.celery_app:celery_app worker --loglevel=info --queues recommendations,ml`.

    2. Remove the two Celery environment variables from the `backend:` service's `environment:` block:
       - Line 70: `CELERY_BROKER_URL: ${DOCKER_CELERY_BROKER_URL:-redis://redis:6379/0}`
       - Line 71: `CELERY_RESULT_BACKEND: ${DOCKER_CELERY_RESULT_BACKEND:-redis://redis:6379/1}`

    Use exact string matching for removal. The backend service block should have no remaining Celery references.
  </action>
  <verify>
    <automated>Select-String -Path "docker-compose.yml" -Pattern "worker:" -SimpleMatch; if ($LASTEXITCODE -eq 0) { "FAIL: worker still present" } else { "PASS: worker removed" }</automated>
  </verify>
  <done>No `worker:` service in docker-compose.yml. No `CELERY_BROKER_URL` or `CELERY_RESULT_BACKEND` env vars in backend service.</done>
</task>

<task type="auto">
  <name>task 2: remove celery dependency and add scikit-learn to pyproject.toml</name>
  <files>backend/pyproject.toml</files>
  <read_first>
    backend/pyproject.toml — full file:
    - dependencies list — remove `"celery>=5.3.4"` at line 22
    - [project.optional-dependencies] → [ml] — add `"scikit-learn>=1.3.0"`
  </read_first>
  <action>
    1. Remove the celery dependency line: delete the exact line `    "celery>=5.3.4",` from the `[project] dependencies` list (line 22). Ensure the comma placement on surrounding lines remains valid JSON-compatible TOML.

    2. Add scikit-learn to [ml] optional deps: insert `    "scikit-learn>=1.3.0",` as a new line in the `[project.optional-dependencies] ml` list (after line 58 `"neo4j>=5.14.0",` or before `"prefect>=2.14.0",`). Maintain alphabetical order within the ml list: scikit-learn comes after neo4j, before prefect.
  </action>
  <verify>
    <automated>if (Select-String -Path "backend/pyproject.toml" -Pattern "celery" -Quiet) { "FAIL: celery still present" } else { "PASS: celery removed" };& if (Select-String -Path "backend/pyproject.toml" -Pattern "scikit-learn" -Quiet) { "PASS: scikit-learn present" } else { "FAIL: scikit-learn missing" }</automated>
  </verify>
  <done>No `celery` in dependencies. `scikit-learn>=1.3.0` present in [ml] optional deps.</done>
</task>

<task type="auto">
  <name>task 3: clean celery references from sentry_config.py, job_store.py, and test files</name>
  <files>
    backend/app/sentry_config.py,
    backend/app/services/job_store.py,
    backend/tests/test_celery.py,
    backend/tests/test_recommendation_lifecycle.py
  </files>
  <read_first>
    backend/app/sentry_config.py — lines 33-38 (CeleryIntegration try/except block)
    backend/app/services/job_store.py — line 52 ("celery_task_id": "" in create_job), line 84 ("celery_task_id": raw.get(...) in get_job)
    backend/tests/test_celery.py — full file (entire file to delete)
    backend/tests/test_recommendation_lifecycle.py — lines 44-55 (test_profile_recommend_requires_auth and test_text_recommend_requires_auth testing endpoints being removed), lines 57-80 (test_job_poll_owner_enforced references celery_task_id), lines 83-117 (test_job_poll_completed_payload_contract references celery_task_id), lines 120-140 (test_job_poll_timed_out_maps_to_504 references celery_task_id)
  </read_first>
  <action>
    1. **sentry_config.py**: Remove the entire `CeleryIntegration` try/except block (lines 33-38):
       ```
       try:
           from sentry_sdk.integrations.celery import CeleryIntegration
       except ImportError:
           pass
       else:
           integrations.append(CeleryIntegration())
       ```
       Remove exactly these 6 lines. Do not touch the RedisIntegration block below it.

    2. **job_store.py**: 
       - Remove `"celery_task_id": "",` from the `create_job` payload dict (line 52)
       - Remove `"celery_task_id": raw.get("celery_task_id") or None,` from the `get_job` return dict (line 84)

    3. **test_celery.py**: DELETE the entire file. It imports from `app.tasks.recommend_tasks` which doesn't exist. No replacement needed — Celery is removed entirely.

    4. **test_recommendation_lifecycle.py**: Remove `"celery_task_id": None,` from each fake job dict:
       - Line 69 (in test_job_poll_owner_enforced)
       - Line 105 (in test_job_poll_completed_payload_contract)
       - Line 132 (in test_job_poll_timed_out_maps_to_504)
       Also remove the two test functions that test removed endpoints:
       - `test_profile_recommend_requires_auth` (lines 44-46): DELETE entire function
       - `test_text_recommend_requires_auth` (lines 49-54): DELETE entire function
       These endpoints are being removed in Plan 01-02.

    Run ruff format on all modified Python files after changes to ensure linting compliance.
  </action>
  <verify>
    <automated>if (Select-String -Path "backend/app/sentry_config.py" -Pattern "CeleryIntegration" -Quiet) { "FAIL: CeleryIntegration still present" } else { "PASS: CeleryIntegration removed" };& if (Select-String -Path "backend/app/services/job_store.py" -Pattern "celery_task_id" -Quiet) { "FAIL: celery_task_id still present" } else { "PASS: celery_task_id removed" };& if (Test-Path "backend/tests/test_celery.py") { "FAIL: test_celery.py still exists" } else { "PASS: test_celery.py deleted" }</automated>
  </verify>
  <done>sentry_config.py has no CeleryIntegration, job_store.py has no celery_task_id, test_celery.py deleted, test_recommendation_lifecycle.py cleaned of celery_task_id references and removed-endpoint tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker host → containers | Container removal reduces attack surface |
| Celery worker → Redis | Worker removed, no more Redis broker connections |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T (Tampering) | pyproject.toml | accept | Removing one dep and adding another has minimal risk — verified by `ruff check` and `pip install` |
| T-01-02 | E (Info Disclosure) | sentry_config.py | accept | CeleryIntegration was only for tracing Celery tasks in Sentry — no PII involved |
</threat_model>

<verification>
1. `grep -c celery backend/app/sentry_config.py` returns 0
2. `grep -c celery_task_id backend/app/services/job_store.py` returns 0
3. `grep -c celery backend/pyproject.toml` returns 0
4. `grep -c scikit-learn backend/pyproject.toml` returns 1
5. `grep -c worker docker-compose.yml` returns 0 (only "worker" in comments should be checked — use `grep -v "^#" | grep -c worker`)
6. `mypy backend/app/sentry_config.py backend/app/services/job_store.py --ignore-missing-imports` passes
</verification>

<success_criteria>
- `make up` starts without attempting to start a worker container
- `docker-compose ps` shows no worker service
- `pip install -e ".[dev,runtime,ml]"` succeeds without celery dependency
- `ruff check backend/app/` passes after modifications
</success_criteria>

<output>
After completion, create `.planning/phases/01-pipeline-data-foundation/01-01-SUMMARY.md`
</output>
